import React from 'react';
import ProductTemplate from '../../components/product-template/product-template';

const id = 'lvzh-140v';  //Значение берётся из поля id продукта в JSON-файлах

export default () => (
    <ProductTemplate name={id}/>
)